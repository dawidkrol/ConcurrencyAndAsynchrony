<Window x:Class="TrainingV1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TrainingV1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="10*"></RowDefinition>
        </Grid.RowDefinitions>
        <ProgressBar x:Name="_progress" Grid.Row="0"></ProgressBar>
        <Button x:Name="_start" Grid.Row="1" Click="_start_Click">Start asynchronic operation</Button>
        <Button x:Name="_cancel" Grid.Row="2" Click="_cancel_Click">Cancel operation</Button>
        <TextBlock x:Name="_tekst" Grid.Row="3"></TextBlock>
    </Grid>
</Window>
