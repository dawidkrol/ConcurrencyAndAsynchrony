<Window x:Class="AsynchronicOperations.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AsynchronicOperations"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>

        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="5*"></RowDefinition>
            <RowDefinition Height="2*"></RowDefinition>
            <RowDefinition Height="2*"></RowDefinition>
            <RowDefinition Height="2*"></RowDefinition>
            <RowDefinition Height="10*"></RowDefinition>
        </Grid.RowDefinitions>
        <ProgressBar Grid.Row="0" x:Name="_progress" BorderBrush="White" Foreground="Black" Background="White"></ProgressBar>
        <Button x:Name="downAsync" Grid.Row="2" Click="downAsync_Click">Download websites async</Button>
        <Button x:Name="downParallel" Grid.Row="3" Click="downParallel_Click">Download websites parallel async</Button>
        <Button x:Name="canel" Grid.Row="4" Click="canel_Click">Canel download</Button>
        <TextBlock x:Name="box" Grid.Row="5" Padding="10,10,10,10"/>
    </Grid>
</Window>
